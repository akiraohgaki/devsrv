/**
 * Server options.
 */
const serverOptions = {
  hostname: '0.0.0.0',
  port: 3000,
  directoryIndex: 'index.html',
  bundle: true,
  playground: true,
  documentRoot: '.',
};

export default serverOptions;
